#!/usr/bin/env bash

tmp="/tmp/clip_tmp_nae18aA6ARaiOF"

if [ "$(uname)" = "Darwin" ]; then
	copy="pbcopy"
else
	copy="xsel --clipboard --input"
fi

nvim -c "startinsert" "$tmp" || exit 1

if [ -e "$tmp" ]; then
	head -c -1 "$tmp" | $copy && command rm "$tmp"
else
	echo -n "" | $copy
fi
