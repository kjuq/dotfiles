# vim: set noexpandtab :

[user]
	name = kjuq
	email = 44694396+kjuq@users.noreply.github.com
	signingkey = 23AA3C90F78458AF
[commit]
	gpgsign = true
	verbose = true

[pull]
	rebase = false
[init]
	defaultBranch = master

[credential]
	helper = cache --timeout=3600

[core]
	pager = "nvim -R -c 'setlocal nonumber norelativenumber'"
	sparsecheckout = true
[color]
	pager = false

[diff]
	tool = nvimdiff
[difftool]
	prompt = false

[merge]
	tool = nvimdiff
[mergetool "nvimdiff"]
	layout = "LOCAL,BASE,REMOTE / MERGED"

[safe]
	directory = /storage/emulated/0/*

; `gh auth login` allows pushing without setting up keys nor this trick
; [url "ssh://git@github.com"]
; 	insteadOf = https://github.com

[include]
	path = ~/.config/git/config_local

; difftool and mergetool
; https://gist.github.com/openjck/93b335dfe5e33955d7dec3c9aec9d609
