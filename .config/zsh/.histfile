exit
ls
exit
set
[[ -x "$(command -v ssh-agent)"\
exit
if [[ -x "$(command -v ssh-agent)"; then echo 'hello'; fi
if [[ -x "$(command -v ssh-agent)"; then; echo 'hello'; fi
if [[ -x "$(command -v ssh-agent)"]]; then echo 'hello'; fi
if [[ -x "$(command -v ssh-agent)" ]]; then echo 'hello'; fi
if [[ -x "$(command -v ssh-agent)" ]]; then echo 'hello'; fi
exit
exit
