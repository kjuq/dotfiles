no-allow-external-cache
default-cache-ttl 43200 # 12 hours
max-cache-ttl 2592000 # one month

# pinentry-mac for browserpass, pinentry-tty for a CLI interface
# pinentry-program /opt/homebrew/bin/pinentry-mac
# pinentry-program /opt/homebrew/bin/pinentry-tty
# pinentry-program /usr/bin/pinentry-curses

# to restart gpg-agent, run:
# gpgconf --kill gpg-agent; and gpgconf --reload gpg-agent; and echo "done"
